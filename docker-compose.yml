services:
  schema-registry:
    build: ./schema-registry
    container_name: schema-registry
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - SOCKET_PATH=/var/sockets/schema.sock
    networks:
      - search-network
    volumes:
      - ./data:/data
      - sockets:/var/sockets

  gateway:
    build: ./gateway
    container_name: gateway
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - SCHEMA_REGISTRY_URL=unix:/var/sockets/schema.sock
      - SHARD_CLUSTER_URL=unix:/var/sockets/shard.sock
      - TEXT_NODE_URL=unix:/var/sockets/text.sock
      - METADATA_NODE_URL=unix:/var/sockets/metadata.sock
      - TAGS_NODE_URL=unix:/var/sockets/tags.sock
    depends_on:
      - schema-registry
      - text-node
      - metadata-node
      - tags-node
      - shard-cluster
    networks:
      - search-network
    volumes:
      - ./data:/data
      - sockets:/var/sockets

  text-node:
    build: ./search-node
    container_name: text-node
    ports:
      - "3001:3001"
    environment:
      - NODE_ID=text-node
      - NODE_SPECIALTY=text
      - PORT=3001
      - SOCKET_PATH=/var/sockets/text.sock
      - SCHEMA_REGISTRY_URL=unix:/var/sockets/schema.sock
      - TEXT_ALGORITHM=${TEXT_ALGORITHM:-inverted}
      - NODE_OPTIONS=--max-old-space-size=4096
    restart: unless-stopped
    networks:
      - search-network
    volumes:
      - ./data:/data
      - sockets:/var/sockets

  metadata-node:
    build: ./search-node
    container_name: metadata-node
    ports:
      - "3002:3002"
    environment:
      - NODE_ID=metadata-node
      - NODE_SPECIALTY=metadata
      - PORT=3002
      - SOCKET_PATH=/var/sockets/metadata.sock
      - SCHEMA_REGISTRY_URL=unix:/var/sockets/schema.sock
    networks:
      - search-network
    volumes:
      - ./data:/data
      - sockets:/var/sockets

  tags-node:
    build: ./search-node
    container_name: tags-node
    ports:
      - "3003:3003"
    environment:
      - NODE_ID=tags-node
      - NODE_SPECIALTY=tags
      - PORT=3003
      - SOCKET_PATH=/var/sockets/tags.sock
      - SCHEMA_REGISTRY_URL=unix:/var/sockets/schema.sock
    networks:
      - search-network
    volumes:
      - ./data:/data
      - sockets:/var/sockets

  shard-cluster:
    build: ./shard-cluster
    container_name: shard-cluster
    ports:
      - "7000:7000"
    environment:
      - NODE_ID=shard-cluster
      - PORT=7000
      - SOCKET_PATH=/var/sockets/shard.sock
      - SHARD_COUNT=4
      - DATA_DIR=/data/shards
    networks:
      - search-network
    volumes:
      - ./data:/data
      - sockets:/var/sockets

  monitoring:
    build: ./monitoring
    container_name: monitoring
    ports:
      - "8080:8080"
    environment:
      - GATEWAY_URL=http://gateway:3000
    depends_on:
      - gateway
    networks:
      - search-network

networks:
  search-network:
    driver: bridge

volumes:
  search-data:
  sockets:
